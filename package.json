{
    "name": "nuxt-static-firebase",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "nuxt",
        "build": "nuxt build",
        "start": "nuxt start",
        "generate": "nuxt generate",
        "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
    },
    "dependencies": {
        "@nuxtjs/firebase": "^6.1.1",
        "firebase": "^7.17.1",
        "nuxt": "^2.14.0"
    },
    "devDependencies": {
        "push-dir": "^0.4.1"
    }
}
